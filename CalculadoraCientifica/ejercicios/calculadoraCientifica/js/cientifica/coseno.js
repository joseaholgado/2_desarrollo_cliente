"use strict";
/**
 * Calcula el coseno de un número.
 * @returns {number} El coseno del número en la variable `num1`.
 */
function coseno() {
    // Obtiene el valor numérico de `num1` y calcula el coseno
    return Math.cos(parseFloat(num1));
}