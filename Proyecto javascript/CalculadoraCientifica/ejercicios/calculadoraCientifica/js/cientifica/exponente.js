"use strict";
/**
 * Calcula el resultado de elevar un número al cuadrado.
 * @returns {number} El resultado de elevar num1 al cuadrado.
 */
function exponente() {
    // Utiliza la función `Math.pow` para elevar num1 al cuadrado (potencia 2)
    // Convierte num1 a un número flotante para asegurarse de que sea un valor numérico
    return Math.pow(parseFloat(num1), 2);
}